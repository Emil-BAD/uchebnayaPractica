<Window x:Class="uchebnayaPractica.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Главное окно системы" Height="600" Width="900" Background="#FAFAFA">
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и кнопка авторизации -->
        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Мероприятия" FontSize="28" FontWeight="Bold" Foreground="#333" DockPanel.Dock="Left"/>
            <Button Content="Авторизация" Width="120" Height="35" HorizontalAlignment="Right" DockPanel.Dock="Right"
                    Click="AuthButton_Click"/>
        </DockPanel>

        <!-- Панель фильтров -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Left" VerticalAlignment="Center">
            <TextBlock Text="Фильтр по направлению:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="DirectionFilter" Width="150" SelectionChanged="FilterChanged" Margin="0,0,20,0"/>

            <TextBlock Text="Дата:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker x:Name="DateFilter" SelectedDateChanged="FilterChanged"/>
        </StackPanel>

        <!-- Список мероприятий -->
        <ListView x:Name="EventList" Grid.Row="2" BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,5" Padding="10" Background="White" CornerRadius="8" BorderBrush="#DDD" BorderThickness="1">
                        <DockPanel>
                            <Image Source="{Binding Logo}" Width="60" Height="60" Margin="0,0,10,0"/>
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="{Binding Direction}" Foreground="Gray"/>
                                <TextBlock Text="{Binding Date, StringFormat='{}{0:dd.MM.yyyy}'}" Foreground="DarkSlateGray"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Window>
